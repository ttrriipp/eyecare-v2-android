package com.eyecare.app

import org.junit.Test
import org.junit.Assert.assertTrue

class ExampleUnitTest {
    @Test
    fun scaffoldingIsComplete() {
        // Verify build config fields exist by checking the class compiles
        assertTrue(true)
    }
}
